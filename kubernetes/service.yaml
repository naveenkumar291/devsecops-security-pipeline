apiVersion: v1
kind: Service
metadata:
  name: devsecops-node-app-service
spec:
  type: NodePort
  selector:
    app: devsecops-node-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080